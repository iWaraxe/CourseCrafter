spring:
  application:
    name: CourseCrafter\

  datasource:
    url: jdbc:postgresql://localhost:5432/coursecrafter
    username: coursecrafter
    password: 19_secret_78
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update        # dev-friendly: creates tables if missing

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      embedding:
        options:
          # Default model: text-embedding-ada-002
          model: text-embedding-3-small
    vectorstore:
      pgvector:
        initialize-schema: false

git:
  repo:
    root: /Users/igorwaraxe/ClaudeCode/CourseUpdater/course-material-repo
    remote: origin
    defaultBranch: main
  auth:
    githubToken: ${GITHUB_PAT}   # read from env-var
